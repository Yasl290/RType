#pragma once

#include <string>
#include <cstdint>

namespace Config {
    namespace Server {
        inline std::string IP = "127.0.0.1";
        inline uint16_t PORT = 4242;
        inline std::string PLAYER_NAME = "Player";
    }
}