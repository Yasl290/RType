#pragma once
#include <vector>
#include <string>

// Frame loading helpers for entity animations
const std::vector<std::string>& getEnemyBasicFrames();
const std::vector<std::string>& getBossFrames();
const std::vector<std::string>& getBossShotFrames();
